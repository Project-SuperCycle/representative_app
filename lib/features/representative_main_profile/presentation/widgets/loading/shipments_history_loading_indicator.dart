import 'package:flutter/material.dart';

class ShipmentsHistoryLoadingIndicator extends StatelessWidget {
  const ShipmentsHistoryLoadingIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
